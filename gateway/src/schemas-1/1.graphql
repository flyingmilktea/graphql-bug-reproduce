union User = UserA | UserB

type Query {
  placeholder: Placeholder
  oneUser(id: ID!): User
}

type Placeholder {
  id: ID!
}

type Mutation {
  oneUser(id: ID!): User
}

type UserA {
  id: ID!
}

type UserB {
  id: ID!
  oneValue: String
  oneUser: User
}
