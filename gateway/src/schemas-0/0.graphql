type Query {
  placeholder: Placeholder
  zeroUser(id: ID): User
}

type Placeholder {
  id: ID!
}

type Mutation {
  zeroUser(id: ID): User
}

type User {
  id: ID!
  zeroValue: String
  user: User!
}
