union User = UserA | UserB

type Query {
  placeholder: Placeholder
  zeroUser(id: ID!): User
}

type Placeholder {
  id: ID!
}

type Mutation {
  zeroUser(id: ID!): User
}

type UserA {
  id: ID!
  zeroValue: String
  zeroUser: User
}

type UserB {
  id: ID!
}
